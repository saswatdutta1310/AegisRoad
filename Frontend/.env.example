# API base URL for the AegisRoad FastAPI backend.
# For local development: leave empty — Vite proxy forwards to localhost:8000 automatically.
# For production (Render): paste your full Render backend URL below.
VITE_API_URL=https://your-backend-name.onrender.com